export declare enum Role {
    USER = "user",
    VENDOR = "vendor"
}
export declare enum OrderStatus {
    PENDING = "pending",
    ON_DELIVERY = "on delivery",
    ASSIGNED = "assigned",
    DELIVERED = "delivered",
    COMPLETED = "completed",
    CANCELLED = "cancelled"
}
